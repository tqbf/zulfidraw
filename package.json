{
  "name": "zulfidraw",
  "version": "1.0.0",
  "description": "An Excalidraw-style whiteboard locked to an isometric grid",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tailwindcss -i input.css -o static/css/app.css --minify",
    "watch": "tailwindcss -i input.css -o static/css/app.css --watch",
    "test": "for t in tests/test_*.js; do echo \"--- $t\"; node \"$t\" || exit 1; done"
  },
  "devDependencies": {
    "htmx.org": "^1.9.12",
    "puppeteer": "^25.3.0",
    "tailwindcss": "^3.4.19"
  }
}
